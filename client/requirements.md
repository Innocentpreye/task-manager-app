## Packages
recharts | Beautiful, responsive charts for the dashboard
date-fns | Date formatting and manipulation for transactions
react-hook-form | Form state management
@hookform/resolvers | Zod validation for forms

## Notes
- App is designed primarily for dark mode as requested. Dark theme CSS variables are set at the `:root` level for seamless integration.
- `useTransactions` hook uses `z.coerce` for robust validation of API responses.
